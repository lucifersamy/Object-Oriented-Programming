#include <iostream>
#include "abstractboard.h"

namespace Game
{
	bool AbstractBoard :: operator == (const AbstractBoard& second)
	{
		exit(1);
	}
	int AbstractBoard :: operator () (int i, int j) const
	{
		exit(1);
	}
}